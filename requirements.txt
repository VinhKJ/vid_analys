# External dependencies for the AI Video Course Analyzer
#
# Install these packages into your Python environment before running
# ``main.py``. You can do this with:
#
#     pip install -r requirements.txt

requests>=2.0.0
# moviepy is required for audio extraction/transcription features
moviepy>=1.0.3

